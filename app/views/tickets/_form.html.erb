<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo, 'Título' %><br />
    <%= f.text_field :titulo, :size => "70" %>
  </div>
  <div class="field">
    <%= f.label :descricao, 'Descrição' %><br />
    <%= f.text_area :descricao, :size => "70x10" %>
  </div>
	<div class="field">
	  <%= f.label :customer %> 
		<%= f.collection_select(:customer_id, Customer.find(:all, :order => :nome), :id, :nome, {:prompt => true}) %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', "javascript:history.back(-1)" %>