<h1>Instituições</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Fone 1</th>
    <th>Fone 2</th>
		<th>Email</th>
		<th>Responsável</th>
		<th></th>
    <th></th>
		<th></th>
		<th></th>
		<th></th>
  </tr>

<% @institutions.each do |institution| %>
  <tr>
    <td><%= institution.nome %></td>
    <td><%= institution.fone1 %></td>
    <td><%= institution.fone2 %></td>
		<td><%= institution.email %></td>
		<td><%= institution.responsavel %></td>
		<td><%= link_to 'Edit', edit_institution_path(institution) %></td>
    <td><%= link_to 'Destroy', institution, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Institution', new_institution_path %>